export type NavItem = {
  label: string;
  href: string;
};

export type Service = {
  title: string;
  description: string;
  blurb: string;
};

export type DoctorDetail = {
  title: string;
  value: string;
};

export type Testimonial = {
  quote: string;
  name: string;
  pet: string;
};

export type BlogPost = {
  title: string;
  category: string;
  excerpt: string;
  date: string;
};

export type FAQItem = {
  question: string;
  answer: string;
};
