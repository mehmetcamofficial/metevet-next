export function escapeCsvCell(value:string|number){const raw=String(value),guarded=/^[=+\-@]/.test(raw)?`'${raw}`:raw;return`"${guarded.replaceAll('"','""')}"`}
export function aggregatedCsv(title:string,range:{startDate:string;endDate:string},metrics:Record<string,string|number>){const safe=escapeCsvCell;return[`Rapor,${safe(title)}`,`Başlangıç,${safe(range.startDate)}`,`Bitiş,${safe(range.endDate)}`,'Metrik,Değer',...Object.entries(metrics).slice(0,1000).map(([k,v])=>`${safe(k)},${safe(v)}`)].join('\r\n')}
