const days=['Pazar','Pazartesi','Salı','Çarşamba','Perşembe','Cuma','Cumartesi'];
export function istanbulHeatmap(rows:Array<{starts_at:string}>){const map:Record<string,number>={};for(const row of rows){const parts=new Intl.DateTimeFormat('en-US',{timeZone:'Europe/Istanbul',weekday:'short',hour:'2-digit',hour12:false}).formatToParts(new Date(row.starts_at)),weekday=parts.find(x=>x.type==='weekday')?.value??'',hour=parts.find(x=>x.type==='hour')?.value??'00',di=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].indexOf(weekday);const key=`${days[Math.max(0,di)]} ${hour}:00`;map[key]=(map[key]??0)+1}return map}
