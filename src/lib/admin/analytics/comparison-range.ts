import type{AnalyticsRange}from"./date-range";
export function previousComparableRange(range:AnalyticsRange):AnalyticsRange{const start=new Date(range.start),end=new Date(range.end),duration=end.getTime()-start.getTime(),previousEnd=new Date(start),previousStart=new Date(start.getTime()-duration),date=(d:Date)=>new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Istanbul'}).format(d);return{preset:'custom',start:previousStart.toISOString(),end:previousEnd.toISOString(),startDate:date(previousStart),endDate:date(new Date(previousEnd.getTime()-1)),label:'Önceki karşılaştırılabilir dönem'}}
