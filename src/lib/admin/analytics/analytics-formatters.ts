export const percent=(part:number,total:number)=>total?Math.round(part/total*1000)/10:0;export const average=(values:number[])=>values.length?Math.round(values.reduce((a,b)=>a+b,0)/values.length*10)/10:0;export function increment(map:Record<string,number>,key:string){map[key]=(map[key]??0)+1}export const trDay=(n:number)=>['Pazar','Pazartesi','Salı','Çarşamba','Perşembe','Cuma','Cumartesi'][n];
