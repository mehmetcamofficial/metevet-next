import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/src/types/database";import type{AnalyticsRange}from"./date-range";import{average,increment,percent,trDay}from"./analytics-formatters";export async function appointmentAnalytics(s:SupabaseClient<Database>,range:AnalyticsRange){const{data,error}=await s.from('appointments').select('id,status,source,service_key,starts_at,ends_at,assigned_user_id').gte('starts_at',range.start).lt('starts_at',range.end).order('starts_at');if(error)throw new Error('Randevu analitiği yüklenemedi.');const rows=data??[],statuses={pending:0,confirmed:0,completed:0,cancelled:0,no_show:0},sources:Record<string,number>={},services:Record<string,number>={},weekdays:Record<string,number>={},hours:Record<string,number>={},staff:Record<string,number>={};for(const x of rows){statuses[x.status]++;increment(sources,x.source);increment(services,x.service_key);const d=new Date(x.starts_at),parts=new Intl.DateTimeFormat('en-US',{timeZone:'Europe/Istanbul',weekday:'short',hour:'2-digit',hour12:false}).formatToParts(d),weekday=trDay(d.getUTCDay());void parts;increment(weekdays,weekday);const hour=new Intl.DateTimeFormat('tr-TR',{timeZone:'Europe/Istanbul',hour:'2-digit',hour12:false}).format(d);increment(hours,`${hour}:00`);increment(staff,x.assigned_user_id??'Atanmamış')}const now=new Date().toISOString(),upcoming=await s.from('appointments').select('id',{count:'exact',head:true}).gte('starts_at',now).neq('status','cancelled');return{total:rows.length,statuses,completionRate:percent(statuses.completed,rows.length),cancellationRate:percent(statuses.cancelled,rows.length),noShowRate:percent(statuses.no_show,rows.length),sources,services,weekdays,hours,staff,averageDurationMinutes:average(rows.map(x=>(new Date(x.ends_at).getTime()-new Date(x.starts_at).getTime())/60000)),upcoming:upcoming.count??0}}
