import type{UserRole}from"@/src/types/database";
export type InsightInput={vaccinesDueSoon:number;vaccinesOverdue:number;parasitesOverdue:number;failedReminders:number;oldDrafts:number;unassignedUpcoming:number;documentFailures:number;noShowRate:number;previousNoShowRate:number;weekdays:Record<string,number>;hours:Record<string,number>;sources:Record<string,number>};
export type Insight={label:string;count:number;href:string;detail:string};
const top=(x:Record<string,number>)=>Object.entries(x).sort((a,b)=>b[1]-a[1])[0];
export function operationalInsights(x:InsightInput,role:UserRole):Insight[]{const clinical=role!=="staff",items:Insight[]=[];if(clinical&&x.vaccinesDueSoon)items.push({label:"7 gün içinde yaklaşan aşılar",count:x.vaccinesDueSoon,href:"/admin/vaccines?due=7d",detail:"Koruyucu bakım planlaması gerektiriyor."});if(clinical&&x.vaccinesOverdue+x.parasitesOverdue)items.push({label:"Geciken koruyucu bakım",count:x.vaccinesOverdue+x.parasitesOverdue,href:"/admin/vaccines?overdue=1",detail:"Geciken operasyon kayıtlarını inceleyin."});if(clinical&&x.failedReminders)items.push({label:"Yeniden işlem bekleyen hatırlatmalar",count:x.failedReminders,href:"/admin/reminders?status=failed",detail:"Başarısız gönderimler güvenli yeniden deneme bekliyor."});if(clinical&&x.oldDrafts)items.push({label:"7 günden eski taslak muayeneler",count:x.oldDrafts,href:"/admin/examinations?status=draft",detail:"Operasyonel kayıt tamamlaması gerekiyor."});if(x.unassignedUpcoming)items.push({label:"Atanmamış yaklaşan randevular",count:x.unassignedUpcoming,href:"/admin/appointments?assigned=none",detail:"Personel ataması bekliyor."});if(x.documentFailures)items.push({label:"Belge üretim hataları",count:x.documentFailures,href:"/admin/documents?status=failed",detail:"Teknik yeniden üretim gerekebilir."});if(x.previousNoShowRate>0&&x.noShowRate>x.previousNoShowRate+5)items.push({label:"No-show oranı önceki dönemden yüksek",count:Math.round(x.noShowRate),href:"/admin/analytics/appointments",detail:`Önceki dönem %${x.previousNoShowRate}.`});const day=top(x.weekdays),hour=top(x.hours),source=top(x.sources);if(day&&hour)items.push({label:`En yoğun zaman: ${day[0]} ${hour[0]}`,count:Math.max(day[1],hour[1]),href:"/admin/calendar",detail:"Takvim kapasitesi için gözlemsel yoğunluk özeti."});if(source)items.push({label:`Baskın randevu kaynağı: ${source[0]}`,count:source[1],href:"/admin/analytics/appointments",detail:"Seçili dönemde en çok randevu üreten kaynak."});return items.slice(0,10)}
