import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/src/types/database";import{CLINICAL_BUCKET}from"./document-storage";export const SIGNED_URL_SECONDS=300;export async function createDocumentSignedUrl(s:SupabaseClient<Database>,path:string){const{data,error}=await s.storage.from(CLINICAL_BUCKET).createSignedUrl(path,SIGNED_URL_SECONDS,{download:true});if(error||!data?.signedUrl)throw new Error("İndirme bağlantısı oluşturulamadı veya süresi doldu.");return data.signedUrl}
