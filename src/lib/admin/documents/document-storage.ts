import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/src/types/database";export const CLINICAL_BUCKET="clinical-documents";export async function uploadClinicalPdf(s:SupabaseClient<Database>,path:string,pdf:Buffer){const{error}=await s.storage.from(CLINICAL_BUCKET).upload(path,pdf,{contentType:"application/pdf",upsert:false,cacheControl:"private, max-age=0"});if(error)throw new Error("Belge güvenli depolamaya yüklenemedi.");}
