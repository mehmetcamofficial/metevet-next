import type{DocumentType,UserRole}from"@/src/types/database";export function canGenerateDocument(role:UserRole,type:DocumentType){return role==="admin"||role==="veterinarian"||type==="appointment_summary"}export const canIncludeInternalNotes=(role:UserRole)=>role==="admin";export const canDeleteDocuments=(role:UserRole)=>role==="admin";export const canArchiveDocument=(role:UserRole,generatedBy:string,userId:string)=>role==="admin"||((role==="veterinarian")&&generatedBy===userId);
