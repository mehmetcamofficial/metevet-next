import Link from "next/link";import type{AppointmentItem}from"../appointments/appointment-card";import{AppointmentStatusBadge}from"../appointments/appointment-status-badge";
export function MobileAgenda({items}:{items:AppointmentItem[]}){return <section className="space-y-3 md:hidden" aria-label="Mobil randevu ajandası">{items.map(i=><Link key={i.id} href={`/admin/appointments/${i.id}`} className="block rounded-xl bg-white p-4"><time className="text-sm font-semibold">{new Intl.DateTimeFormat("tr-TR",{timeZone:"Europe/Istanbul",dateStyle:"medium",timeStyle:"short"}).format(new Date(i.starts_at))}</time><p className="mt-2">{i.pet} · {i.owner}</p><div className="mt-2"><AppointmentStatusBadge status={i.status}/></div></Link>)}</section>}
