import Link from "next/link";import type{AppointmentItem}from"../appointments/appointment-card";import{AppointmentStatusBadge}from"../appointments/appointment-status-badge";
export function DayView({items}:{items:AppointmentItem[]}){return <section className="hidden rounded-2xl bg-white p-5 md:block"><h2 className="font-semibold">Günlük Program</h2><ol className="mt-4 divide-y">{items.map(i=><li key={i.id} className="grid grid-cols-[100px_1fr_auto] items-center gap-4 py-4"><time>{new Intl.DateTimeFormat("tr-TR",{timeZone:"Europe/Istanbul",hour:"2-digit",minute:"2-digit"}).format(new Date(i.starts_at))}</time><Link href={`/admin/appointments/${i.id}`} className="font-medium hover:underline">{i.pet} · {i.owner}</Link><AppointmentStatusBadge status={i.status}/></li>)}</ol></section>}
