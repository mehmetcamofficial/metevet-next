import Link from "next/link";
export function CalendarToolbar({ view, anchor, previous, next, today }: { view: "day"|"week"|"month"; anchor:string; previous:string; next:string; today:string }) { return <div className="flex flex-wrap items-center justify-between gap-3 rounded-xl bg-white p-4"><div className="flex gap-2"><Link className="rounded-lg border px-3 py-2" href={`/admin/calendar?view=${view}&date=${previous}`} aria-label="Önceki dönem">←</Link><Link className="rounded-lg border px-3 py-2" href={`/admin/calendar?view=${view}&date=${today}`}>Bugün</Link><Link className="rounded-lg border px-3 py-2" href={`/admin/calendar?view=${view}&date=${next}`} aria-label="Sonraki dönem">→</Link></div><strong>{anchor}</strong><div className="flex gap-1" aria-label="Takvim görünümü">{(["day","week","month"] as const).map(v=><Link key={v} aria-current={v===view?"page":undefined} className={`rounded-lg px-3 py-2 ${v===view?"bg-[#0d2922] text-white":"border"}`} href={`/admin/calendar?view=${v}&date=${anchor}`}>{v==="day"?"Gün":v==="week"?"Hafta":"Ay"}</Link>)}</div></div> }
