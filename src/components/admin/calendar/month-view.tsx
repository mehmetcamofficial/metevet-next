import Link from "next/link";import type{AppointmentItem}from"../appointments/appointment-card";import{AppointmentStatusBadge}from"../appointments/appointment-status-badge";
export function MonthView({items,days}:{items:AppointmentItem[];days:string[]}){return <section className="hidden grid-cols-7 gap-px overflow-hidden rounded-2xl bg-[#0d2922]/10 md:grid">{days.map(day=>{const list=items.filter(i=>new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Istanbul"}).format(new Date(i.starts_at))===day);return <div key={day} className="min-h-28 bg-white p-2"><time className="text-xs font-semibold">{day.slice(-2)}</time>{list.slice(0,3).map(i=><Link key={i.id} href={`/admin/appointments/${i.id}`} className="mt-2 block truncate text-xs hover:underline">{i.pet} <AppointmentStatusBadge status={i.status}/></Link>)}{list.length>3?<p className="mt-1 text-xs">+{list.length-3} daha</p>:null}</div>})}</section>}
