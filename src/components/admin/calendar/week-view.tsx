import Link from "next/link";import type{AppointmentItem}from"../appointments/appointment-card";import{AppointmentStatusBadge}from"../appointments/appointment-status-badge";
export function WeekView({items}:{items:AppointmentItem[]}){const groups=new Map<string,AppointmentItem[]>();for(const i of items){const key=new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Istanbul"}).format(new Date(i.starts_at));groups.set(key,[...(groups.get(key)??[]),i]);}return <section className="hidden grid-cols-7 gap-2 md:grid">{Array.from(groups.entries()).map(([day,list])=><div key={day} className="min-h-52 rounded-xl bg-white p-3"><h2 className="text-sm font-semibold">{new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium"}).format(new Date(`${day}T12:00:00+03:00`))}</h2>{list.map(i=><Link key={i.id} href={`/admin/appointments/${i.id}`} className="mt-3 block rounded-lg border p-2 text-xs"><span className="block font-semibold">{new Intl.DateTimeFormat("tr-TR",{timeZone:"Europe/Istanbul",hour:"2-digit",minute:"2-digit"}).format(new Date(i.starts_at))} {i.pet}</span><span className="mt-1 block"><AppointmentStatusBadge status={i.status}/></span></Link>)}</div>)}</section>}
