import type{ReminderStatus}from"@/src/types/database";export function ReminderTimeline({item}:{item:{created_at:string;status:ReminderStatus;sent_at:string|null;failed_at:string|null;cancelled_at:string|null}}){const rows=[["Oluşturuldu",item.created_at],["Gönderildi",item.sent_at],["Başarısız",item.failed_at],["İptal",item.cancelled_at]].filter((x):x is[string,string]=>Boolean(x[1]));return <ol className="space-y-3 border-l pl-5">{rows.map(([label,date])=><li key={label}><b>{label}</b><time className="block text-sm text-[#526a64]">{new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium",timeStyle:"short",timeZone:"Europe/Istanbul"}).format(new Date(date))}</time></li>)}</ol>}
