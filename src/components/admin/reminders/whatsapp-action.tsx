"use client";import{useState}from"react";export function WhatsAppAction({url,message,markSent}:{url:string|null;message:string;markSent:()=>Promise<void>}){const[copied,setCopied]=useState(false);return <div className="flex flex-wrap gap-3"><button type="button" onClick={async()=>{await navigator.clipboard.writeText(message);setCopied(true)}} className="rounded border px-4 py-2">{copied?"Kopyalandı":"Kopyala"}</button>{url?<a href={url} target="_blank" rel="noopener noreferrer" className="rounded bg-[#0d2922] px-4 py-2 text-white">WhatsApp’ı Aç</a>:<span role="alert" className="text-sm text-red-700">Geçerli telefon numarası yok.</span>}<form action={markSent}><button className="rounded border border-emerald-700 px-4 py-2 text-emerald-800">Manuel gönderimi onayla</button></form></div>}
