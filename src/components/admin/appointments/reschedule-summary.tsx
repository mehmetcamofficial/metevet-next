export function RescheduleSummary({ original, current }: { original?: { date: string; start: string; end: string }; current: { date: string; start: string; end: string } }) { if (!original || (original.date === current.date && original.start === current.start && original.end === current.end)) return null; return <div className="rounded-xl border border-amber-300 bg-amber-50 p-4" role="status"><h3 className="font-semibold">Yeniden planlama özeti</h3><p className="mt-2 text-sm">Önceki: {original.date} {original.start}–{original.end}</p><p className="text-sm">Yeni: {current.date} {current.start}–{current.end}</p></div>; }
