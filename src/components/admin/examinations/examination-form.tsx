"use client";import{useActionState,useEffect,useMemo,useState}from"react";import type{ExaminationFormState}from"@/app/admin/examinations/actions";import{visitTypeLabels,visitTypes}from"@/src/lib/admin/examinations";import{VitalsFields}from"./vitals-fields";type Owner={id:string;full_name:string};type Pet={id:string;owner_id:string;name:string};type Vet={id:string;full_name:string};type Appointment={id:string;pet_id:string;starts_at:string};type Initial=Record<string,unknown>&{ownerId:string;petId:string;appointmentId:string|null;veterinarianId:string;visitType:string};const initialState={message:null};const cls="mt-2 w-full rounded-lg border border-[#0d2922]/20 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[#cda85f]/50";
export function ExaminationForm({action,owners,pets,veterinarians,appointments,initial}:{action:(s:ExaminationFormState,f:FormData)=>Promise<ExaminationFormState>;owners:Owner[];pets:Pet[];veterinarians:Vet[];appointments:Appointment[];initial?:Initial}){const[state,formAction,pending]=useActionState(action,initialState);const[owner,setOwner]=useState(initial?.ownerId??"");const petOptions=useMemo(()=>pets.filter(p=>p.owner_id===owner),[pets,owner]);useEffect(()=>{const first=Object.keys(state.errors??{})[0];if(first)document.getElementById(first)?.focus();},[state.errors]);const textFields=[['chiefComplaint','Başlıca Şikâyet'],['history','Öykü'],['examinationFindings','Muayene Bulguları'],['assessment','Değerlendirme'],['diagnosis','Tanı'],['procedures','Uygulanan İşlemler'],['treatmentPlan','Tedavi Planı'],['medicationsNotes','İlaç Notları'],['recommendations','Öneriler'],['internalNotes','Dahili Notlar']] as const;return <form action={formAction} className="space-y-7" noValidate><div className="grid gap-5 sm:grid-cols-2"><Field id="ownerId" label="Hayvan Sahibi"><select id="ownerId" className={cls} value={owner} onChange={e=>setOwner(e.target.value)}><option value="">Seçiniz</option>{owners.map(o=><option key={o.id} value={o.id}>{o.full_name}</option>)}</select></Field><Field id="petId" label="Hayvan" error={state.errors?.petId}><select id="petId" name="petId" className={cls} defaultValue={initial?.petId??""}><option value="">Seçiniz</option>{petOptions.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></Field><Field id="appointmentId" label="Bağlı Randevu" error={state.errors?.appointmentId}><select id="appointmentId" name="appointmentId" className={cls} defaultValue={initial?.appointmentId??""}><option value="">Bağlantı yok</option>{appointments.filter(a=>!initial?.petId||a.pet_id===initial.petId).map(a=><option key={a.id} value={a.id}>{new Intl.DateTimeFormat("tr-TR",{dateStyle:"short",timeStyle:"short"}).format(new Date(a.starts_at))}</option>)}</select></Field><Field id="veterinarianId" label="Veteriner Hekim" error={state.errors?.veterinarianId}><select id="veterinarianId" name="veterinarianId" className={cls} defaultValue={initial?.veterinarianId??""}><option value="">Seçiniz</option>{veterinarians.map(v=><option key={v.id} value={v.id}>{v.full_name}</option>)}</select></Field><Field id="visitType" label="Ziyaret Türü" error={state.errors?.visitType}><select id="visitType" name="visitType" className={cls} defaultValue={initial?.visitType??"general_exam"}>{visitTypes.map(v=><option key={v} value={v}>{visitTypeLabels[v]}</option>)}</select></Field><Field id="followUpAt" label="Kontrol Tarihi"><input id="followUpAt" name="followUpAt" type="datetime-local" className={cls} defaultValue={String(initial?.followUpAt??"")}/></Field></div><VitalsFields initial={initial as Record<string,number|null>|undefined} errors={state.errors}/><div className="grid gap-5 lg:grid-cols-2">{textFields.map(([name,label])=><Field key={name} id={name} label={label}><textarea id={name} name={name} className={`${cls} min-h-28`} defaultValue={String(initial?.[name]??"")}/></Field>)}</div><section className="rounded-xl border border-amber-300 bg-amber-50 p-4"><h2 className="font-semibold">Finalizasyon Özeti</h2><p className="mt-2 text-sm">Finalizasyon sonrası kayıt salt okunur olur. Hayvan, veteriner, ziyaret türü ve klinik bölümlerini kontrol edin.</p></section>{state.message?<p aria-live="polite" className="rounded-lg bg-red-50 p-3 text-sm text-red-800">{state.message}</p>:null}<div className="flex flex-wrap gap-3"><button name="intent" value="draft" disabled={pending} className="rounded-lg border px-5 py-3 font-semibold">Taslak Kaydet</button><button name="intent" value="finalize" disabled={pending} onClick={e=>{if(!window.confirm("Kaydı final hale getirmek istediğinize emin misiniz?"))e.preventDefault();}} className="rounded-lg bg-[#0d2922] px-5 py-3 font-semibold text-white">Final Kaydet</button></div></form>}
function Field({id,label,error,children}:{id:string;label:string;error?:string;children:React.ReactNode}){return <div><label htmlFor={id} className="text-sm font-medium">{label}</label>{children}{error?<p id={`${id}-error`} className="mt-1 text-sm text-red-700">{error}</p>:null}</div>}
