const cls="mt-2 w-full rounded-lg border border-[#0d2922]/20 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[#cda85f]/50";export function VitalsFields({initial,errors}:{initial?:Record<string,number|null>;errors?:Record<string,string>}){const fields=[['weight','Ağırlık (kg)','0.01'],['temperature','Sıcaklık (°C)','0.1'],['heartRate','Kalp Hızı','1'],['respiratoryRate','Solunum Hızı','1']] as const;return <fieldset><legend className="text-lg font-semibold">Vital Bulgular</legend><div className="mt-4 grid gap-4 sm:grid-cols-4">{fields.map(([name,label,step])=><div key={name}><label htmlFor={name} className="text-sm font-medium">{label}</label><input id={name} name={name} type="number" step={step} defaultValue={initial?.[name]??""} className={cls} aria-invalid={Boolean(errors?.[name])} aria-describedby={errors?.[name]?`${name}-error`:undefined}/>{errors?.[name]?<p id={`${name}-error`} className="mt-1 text-sm text-red-700">{errors[name]}</p>:null}</div>)}</div></fieldset>}
