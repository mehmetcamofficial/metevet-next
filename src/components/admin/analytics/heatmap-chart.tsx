export function HeatmapChart({data}:{data:Record<string,number>}){const rows=Object.entries(data).sort(),max=Math.max(1,...rows.map(x=>x[1]));if(!rows.length)return <p className="text-sm text-[#526a64]">Bu dönem için yoğunluk verisi yok.</p>;return <figure><figcaption className="mb-3 text-sm text-[#526a64]">Europe/Istanbul saat diliminde hafta günü ve saat yoğunluğu.</figcaption><div className="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4" aria-hidden="true">{rows.map(([label,value])=><div key={label} className="rounded border p-2 text-xs" style={{backgroundColor:`rgba(13,41,34,${.08+.72*value/max})`,color:value/max>.55?'white':'#0d2922'}}>{label}<b className="float-right">{value}</b></div>)}</div><table className="sr-only"><caption>Randevu yoğunluğu tablo karşılığı</caption><thead><tr><th>Zaman</th><th>Randevu</th></tr></thead><tbody>{rows.map(([k,v])=><tr key={k}><td>{k}</td><td>{v}</td></tr>)}</tbody></table></figure>}
