import Link from"next/link";export function AlertList({items}:{items:Array<{label:string;count:number;href:string}>}){const active=items.filter(x=>x.count>0);return active.length?<ul className="space-y-3">{active.map(x=><li key={x.label}><Link href={x.href} className="flex justify-between rounded-lg border border-amber-300 bg-amber-50 p-3 focus-visible:outline-2 focus-visible:outline-[#cda85f]"><span>{x.label}</span><b>{x.count}</b></Link></li>)}</ul>:<p className="text-sm text-[#526a64]">Şu anda dikkat gerektiren kayıt yok.</p>}
