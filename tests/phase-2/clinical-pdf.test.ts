import test from"node:test";import assert from"node:assert/strict";import{readFileSync,existsSync,statSync}from"node:fs";import{clinicalDocumentFileName,sanitizeDocumentFileName}from"../../src/lib/admin/documents/document-file-name.ts";
const read=(p:string)=>readFileSync(p,"utf8"),layout=()=>read("src/components/documents/document-layout.tsx"),data=()=>read("src/lib/admin/documents/document-data.ts");
test("Turkish Unicode font is locally registered in four weights",()=>{const x=read("src/lib/admin/documents/pdf-fonts.ts");assert.match(x,/Noto Sans/);for(const w of [400,500,600,700])assert.match(x,new RegExp(`fontWeight: ?${w}`));assert.doesNotMatch(x,/https?:\/\//)});
test("font and OFL license assets are bundled",()=>{for(const p of ["src/assets/fonts/noto-sans/NotoSans-Regular.ttf","src/assets/fonts/noto-sans/NotoSans-Medium.ttf","src/assets/fonts/noto-sans/NotoSans-SemiBold.ttf","src/assets/fonts/noto-sans/NotoSans-Bold.ttf","src/assets/fonts/noto-sans/OFL.txt"]){assert.ok(existsSync(p));assert.ok(statSync(p).size>1000)}});
test("shared layout explicitly uses Unicode font and never Helvetica",()=>{assert.match(read("src/components/documents/pdf-primitives.tsx"),/PDF_FONT_FAMILY/);assert.doesNotMatch(layout(),/Helvetica/)});
test("all document types share the base layout",()=>{for(const p of ["examination-document.tsx","appointment-document.tsx","vaccine-card-document.tsx","preventive-care-document.tsx","pet-summary-document.tsx","follow-up-document.tsx"])assert.match(read(`src/components/documents/${p}`),/ClinicalDocument/)});
test("Turkish enum keys receive readable labels",()=>{const x=data();for(const label of ["Genel Muayene","Kontrol","Dişi","Erkek","Doğrudan Başvuru"])assert.match(x,new RegExp(label))});
test("empty values never serialize null or undefined",()=>{assert.match(data(),/Belirtilmemiş/);assert.doesNotMatch(layout(),/>null</);assert.doesNotMatch(layout(),/>undefined</)});
test("long text is not placed in fixed-height boxes",()=>{const x=read("src/components/documents/pdf-primitives.tsx");assert.doesNotMatch(x,/height:\s*\d+/);assert.match(x,/lineHeight/)});
test("internal notes are excluded unless explicitly requested",()=>{const x=data();assert.match(x,/if\(includeInternal\)/);assert.doesNotMatch(x,/includeInternal=true/)});
test("internal notes permission is revalidated server-side",()=>{const x=read("app/admin/documents/actions.ts");assert.match(x,/canIncludeInternalNotes/);assert.match(x,/internal && !canIncludeInternalNotes/)});
test("examination summary requires a final record",()=>assert.match(data(),/x\.status!=="finalized"/));
test("clinician attribution requires an active veterinarian",()=>{const x=data();assert.match(x,/v\.role!=="veterinarian"/);assert.match(x,/v\.status!=="active"/)});
test("readable filename is safe and contains document and pet slugs",()=>{assert.equal(clinicalDocumentFileName("MV-20260714-56ED78AD","examination_summary","Sütlaç"),"MV-20260714-56ED78AD-muayene-ozeti-sutlac.pdf");assert.equal(sanitizeDocumentFileName("../kötü:ad"),"..-kotu-ad.pdf")});
test("filename does not include contact information",()=>{const x=clinicalDocumentFileName("MV-1","vaccination_card","Sütlaç");assert.doesNotMatch(x,/@|\+90|506585/)});
test("footer carries clinic, confidentiality and document number",()=>{const x=read("src/components/documents/document-footer.tsx");assert.match(x,/data\.reference/);assert.match(x,/Gizli klinik belge/);assert.match(x,/data\.clinic\.name/)});
test("document type and status labels remain localized",()=>{assert.match(read("src/lib/admin/documents/document-types.ts"),/Muayene Özeti/);const x=read("src/components/admin/documents/document-status-badge.tsx");assert.match(x,/Hazır/);assert.match(x,/Arşivlendi/)});
test("patient identity and vital units are present",()=>{const x=data();for(const k of ["species","breed","sex","birthDate","age","microchip","kg","°C","atım\/dk","solunum\/dk"])assert.match(x,new RegExp(k))});
test("no PDF renderer uses a remote font or raw storage path",()=>{for(const p of ["src/components/documents/document-layout.tsx","src/components/documents/pdf-primitives.tsx","src/lib/admin/documents/pdf-fonts.ts"])assert.doesNotMatch(read(p),/https?:\/\/|storage_path|signed_url/)});
test("font registration stays outside client components",()=>{assert.doesNotMatch(read("src/lib/admin/documents/pdf-fonts.ts"),/use client/);assert.doesNotMatch(layout(),/use client/)});
test("fixture PDFs were rendered with A4 shared layout",()=>{for(const p of ["output/pdf/muayene-ozeti-sutlac.pdf","output/pdf/asi-karnesi-sutlac.pdf","output/pdf/koruyucu-saglik-sutlac.pdf"]){assert.ok(existsSync(p));assert.ok(statSync(p).size>5000)}});
