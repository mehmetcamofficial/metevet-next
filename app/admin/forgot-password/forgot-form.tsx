"use client";import Link from"next/link";import{useActionState}from"react";import{requestPasswordRecovery,type RecoveryState}from"./actions";export function ForgotForm(){const[state,action,pending]=useActionState<RecoveryState,FormData>(requestPasswordRecovery,{message:""});return <form action={action} className="mt-7 space-y-5"><label className="block">E-posta<input name="email" type="email" required autoComplete="email" className="mt-2 w-full rounded border p-3"/></label>{state.message?<p role="status" className="rounded bg-emerald-50 p-3 text-sm">{state.message}</p>:null}<button disabled={pending} className="w-full rounded bg-[#0d2922] p-3 text-white">{pending?"Gönderiliyor…":"Yenileme Bağlantısı Gönder"}</button><p className="text-center text-sm"><Link href="/admin/login" className="underline">Girişe dön</Link></p></form>}
