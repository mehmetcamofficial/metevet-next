import type{ReactNode}from"react";import{notFound}from"next/navigation";import{ExaminationPreventiveActions}from"@/src/components/admin/vaccines/examination-preventive-actions";import{requireStaff}from"@/src/lib/auth/require-staff";import{createClient}from"@/src/lib/supabase/server";export default async function Layout({children,params}:{children:ReactNode;params:Promise<{id:string}>}){const{id}=await params,session=await requireStaff(),s=await createClient();if(!s)notFound();const{data}=await s.from("examinations").select("pet_id,veterinarian_id,appointment_id").eq("id",id).single();if(!data)notFound();return <>{children}<ExaminationPreventiveActions petId={data.pet_id} vetId={data.veterinarian_id} appointmentId={data.appointment_id} role={session.profile.role}/></>}
