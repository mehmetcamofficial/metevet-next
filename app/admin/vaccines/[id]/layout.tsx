import type{ReactNode}from"react";import{notFound}from"next/navigation";import{requireStaff}from"@/src/lib/auth/require-staff";import{createClient}from"@/src/lib/supabase/server";import{DocumentCreateLinks}from"@/src/components/admin/documents/entity-documents";export default async function Layout({children,params}:{children:ReactNode;params:Promise<{id:string}>}){await requireStaff();const{id}=await params,s=await createClient();if(!s)notFound();const{data}=await s.from("vaccination_records").select("pet_id").eq("id",id).single();if(!data)notFound();return <>{children}<DocumentCreateLinks links={[{label:"Aşı Belgesi Oluştur",type:"vaccination_card",source:data.pet_id,preview:`/admin/documents/vaccine-card/${data.pet_id}`}]}/></>}
