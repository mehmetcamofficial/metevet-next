import{notFound}from"next/navigation";import{createParasite}from"../actions";import{AdminShell}from"@/src/components/admin/admin-shell";import{ParasiteForm}from"@/src/components/admin/parasites/parasite-form";import{canWritePreventive}from"@/src/lib/admin/preventive-care";import{requireStaff}from"@/src/lib/auth/require-staff";import{createClient}from"@/src/lib/supabase/server";export default async function Page({searchParams}:{searchParams:Promise<{pet?:string;vet?:string}>}){const session=await requireStaff();if(!canWritePreventive(session.profile.role))notFound();const p=await searchParams,s=await createClient();if(!s)notFound();const[pets,vets]=await Promise.all([s.from("pets").select("id,name").is("archived_at",null),s.from("profiles").select("id,full_name,role")]);return <AdminShell session={session}><h1 className="text-3xl font-semibold">Yeni Parazit Uygulaması</h1><section className="mt-6 rounded-2xl bg-white p-6"><ParasiteForm action={createParasite} pets={pets.data??[]} vets={(vets.data??[]).filter(x=>x.role!=="staff")} initial={{petId:p.pet??"",veterinarianId:p.vet??(session.profile.role==="veterinarian"?session.id:"")}}/></section></AdminShell>}
