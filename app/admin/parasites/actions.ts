"use server";import{revalidatePath}from"next/cache";import{redirect}from"next/navigation";import{requireStaff}from"@/src/lib/auth/require-staff";import{canWritePreventive,preventiveStatuses}from"@/src/lib/admin/preventive-care";import{canPermanentlyDelete}from"@/src/lib/admin/permissions";import{createClient}from"@/src/lib/supabase/server";export type ParasiteState={message:string|null;errors?:Record<string,string>};
async function save(id:string|null,_s:ParasiteState,fd:FormData):Promise<ParasiteState>{const session=await requireStaff();if(!canWritePreventive(session.profile.role))return{message:"Yetkiniz bulunmuyor."};const v={petId:String(fd.get("petId")??""),veterinarianId:String(fd.get("veterinarianId")??"")||null,treatmentType:String(fd.get("treatmentType")??""),productName:String(fd.get("productName")??"").trim(),batchNumber:String(fd.get("batchNumber")??"").trim()||null,administrationDate:String(fd.get("administrationDate")??""),nextDueDate:String(fd.get("nextDueDate")??"")||null,status:String(fd.get("status")??"completed"),notes:String(fd.get("notes")??"").trim()||null,internalNotes:String(fd.get("internalNotes")??"").trim()||null};const errors:Record<string,string>={};if(!v.petId)errors.petId="Hayvan zorunludur.";if(!["internal","external","combined"].includes(v.treatmentType))errors.treatmentType="Tedavi türü zorunludur.";if(!v.productName)errors.productName="Ürün adı zorunludur.";if(!v.administrationDate)errors.administrationDate="Tarih zorunludur.";if(!preventiveStatuses.includes(v.status as never))errors.status="Geçerli durum seçin.";const s=await createClient();if(!s)return{message:"İşlem başarısız."};const{data:pet}=await s.from("pets").select("owner_id").eq("id",v.petId).single();if(!pet)errors.petId="Geçerli hayvan seçin.";if(Object.keys(errors).length)return{message:"Alanları düzeltin.",errors};const rec={pet_id:v.petId,owner_id:pet!.owner_id,veterinarian_id:v.veterinarianId,treatment_type:v.treatmentType as"internal"|"external"|"combined",product_name:v.productName,batch_number:v.batchNumber,administration_date:new Date(v.administrationDate).toISOString(),next_due_date:v.nextDueDate?new Date(v.nextDueDate).toISOString():null,status:v.status as"scheduled"|"completed"|"expired"|"cancelled",notes:v.notes,internal_notes:v.internalNotes};const r=id?await s.from("parasite_records").update(rec).eq("id",id).select("id").single():await s.from("parasite_records").insert(rec).select("id").single();if(r.error||!r.data)return{message:"İşlem başarısız."};await s.from("audit_logs").insert({actor_user_id:session.id,action:id?"parasite_updated":rec.status==="completed"?"parasite_completed":"parasite_created",entity_type:"parasite_record",entity_id:r.data.id,metadata:{status:rec.status}});revalidatePath("/admin/parasites");revalidatePath(`/admin/pets/${v.petId}`);redirect(`/admin/parasites/${r.data.id}`)}export async function createParasite(s:ParasiteState,f:FormData){return save(null,s,f)}export async function updateParasite(id:string,s:ParasiteState,f:FormData){return save(id,s,f)}
async function life(id:string,a:"archive"|"restore"|"delete"){const session=await requireStaff();if(!canPermanentlyDelete(session.profile.role))throw new Error("Yetkiniz yok.");const s=await createClient();if(!s)throw new Error("İşlem başarısız.");const{data}=await s.from("parasite_records").select("pet_id").eq("id",id).single();if(!data)throw new Error("Kayıt yok.");const r=a==="delete"?await s.from("parasite_records").delete().eq("id",id):await s.from("parasite_records").update({archived_at:a==="archive"?new Date().toISOString():null}).eq("id",id);if(r.error)throw new Error("İşlem başarısız.");await s.from("audit_logs").insert({actor_user_id:session.id,action:`parasite_${a}d`,entity_type:"parasite_record",entity_id:id,metadata:{}});revalidatePath("/admin/parasites");revalidatePath(`/admin/pets/${data.pet_id}`);if(a==="delete")redirect("/admin/parasites")}export async function archiveParasite(id:string){await life(id,"archive")}export async function restoreParasite(id:string){await life(id,"restore")}export async function deleteParasite(id:string){await life(id,"delete")}
