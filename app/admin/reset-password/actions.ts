"use server";import{requireStaff}from"@/src/lib/auth/require-staff";import{createServerActionClient}from"@/src/lib/supabase/server-action";export async function recordPasswordChanged(){const session=await requireStaff(),s=await createServerActionClient();if(s)await s.from("audit_logs").insert({actor_user_id:session.id,action:"password_changed",entity_type:"profile",entity_id:session.id,metadata:{method:"self_service"}})}
