import{requireStaff}from"@/src/lib/auth/require-staff";import{DocumentGenerateForm}from"@/src/components/admin/documents/document-generate-form";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const session=await requireStaff(),q=await searchParams;return <><h1 className="text-3xl font-semibold">Belge Oluştur</h1><p className="mt-2 text-[#526a64]">Kaynak kayıt sunucuda yeniden doğrulanır; PDF tarayıcıda üretilmez.</p><DocumentGenerateForm role={session.profile.role} initialType={q.type} initialSource={q.source}/></>}
