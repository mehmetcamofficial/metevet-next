"use client";import{useActionState}from"react";import{updateOwnProfile,type ProfileState}from"./actions";export function ProfileForm({fullName,phone}:{fullName:string;phone:string|null}){const[state,action,pending]=useActionState<ProfileState,FormData>(updateOwnProfile,{message:null});return <form action={action} className="space-y-4"><label className="block">Ad Soyad<input name="fullName" defaultValue={fullName} required className="mt-1 w-full rounded border p-3"/></label><label className="block">Telefon<input name="phone" defaultValue={phone??""} className="mt-1 w-full rounded border p-3"/></label>{state.message?<p role="status" className="text-sm">{state.message}</p>:null}<button disabled={pending} className="rounded border px-4 py-2">Profili Kaydet</button></form>}
