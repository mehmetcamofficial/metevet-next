import{PasswordForm}from"@/src/components/admin/personnel/password-form";import{requireStaff}from"@/src/lib/auth/require-staff";import{createClient}from"@/src/lib/supabase/server";import{ProfileForm}from"./profile-form";const labels={admin:"Yönetici",veterinarian:"Veteriner Hekim",staff:"Personel"}as const;export default async function Page(){const session=await requireStaff(),s=await createClient();const[{data},{data:privateProfile}]=s?await Promise.all([s.from("profiles").select("full_name,role").eq("id",session.id).single(),s.from("personnel_private_profiles").select("email,phone").eq("id",session.id).maybeSingle()]):[{data:null},{data:null}];if(!data)return <p role="alert">Profil yüklenemedi.</p>;return <><h1 className="text-3xl font-semibold">Profilim</h1><div className="mt-6 grid gap-6 lg:grid-cols-2"><section className="rounded-xl bg-white p-6"><h2 className="mb-4 text-xl font-semibold">Kişisel Bilgiler</h2><p className="mb-4 text-sm">{privateProfile?.email??session.email} · {labels[data.role]}</p><ProfileForm fullName={data.full_name} phone={privateProfile?.phone??null}/></section><section className="rounded-xl bg-white p-6"><h2 className="mb-4 text-xl font-semibold">Şifre Değiştir</h2><PasswordForm/></section></div></>}
